import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eysayisaaa.epicenterdsp',
  appName: 'Epicenter DSP Movil',
  webDir: 'dist'
};

export default config;
